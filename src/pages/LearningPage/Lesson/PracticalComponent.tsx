import React from 'react'
import { IPrascticalComponent } from '.'

const PracticalComponent: React.FC<IPrascticalComponent> = ({id, title, task_title, task_type, completed}) => {
    return(
        <div>

        </div>
    )
}

export default PracticalComponent